# build/

مجلد موارد البناء الخاص بـ electron-builder.

- `clean.js` — يحذف مخرجات البناء في `release/` فقط، ولا يمس مجلد المصدر إطلاقاً.
- `icon.ico` — أيقونة التطبيق (اختيارية). ضع هنا ملف ICO بمقاس 256×256 وسيلتقطه
  electron-builder تلقائياً. بدونه يستخدم البناء أيقونة Electron الافتراضية.
